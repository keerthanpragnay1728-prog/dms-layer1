# Dependency policy: nothing beyond this agreed set without asking first
# (torch, torchvision, opencv-python, numpy, mediapipe, pyyaml, matplotlib).
# Kaggle/Colab preinstall most of these, so `pip install -r` is fast there.

# milestone 1+ (mapping, loader, visualisation)
numpy
# Pinned below 5: OpenCV 5.x removed the Haar CascadeClassifier API that the
# milestone-3 face detector depends on. 4.x is what Kaggle ships anyway. The
# cascade XML itself is vendored in assets/ because even 4.x wheel data files
# are not guaranteed present in every build.
opencv-python>=4.8,<5
pyyaml
matplotlib

# milestone 4+ (model + training) — uncomment when training starts
# torch
# torchvision

# milestone 6+ (MediaPipe detector for the ablation)
# mediapipe
